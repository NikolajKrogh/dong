import { createBdd } from "playwright-bdd";

const { Given, When, Then } = createBdd();

Given("the account auth flow is available", async () => {
  void 0;
});

When("a new user signs up", async () => {
  void 0;
});

When("a returning user signs in", async () => {
  void 0;
});

When("the user requests a password reset", async () => {
  void 0;
});

When("the user opens a recovery link", async () => {
  void 0;
});

When("the user submits a new password", async () => {
  void 0;
});

When("a signed-out user opens the owner gate", async () => {
  void 0;
});

Then("the user should be routed to display-name onboarding", async () => {
  void 0;
});

Then("the account should be restored", async () => {
  void 0;
});

Then("the reset confirmation should be visible", async () => {
  void 0;
});

Then("the user should return to the sign in flow", async () => {
  void 0;
});

Then("the app should route them through auth and back to settings", async () => {
  void 0;
});