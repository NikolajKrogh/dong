import splashAnimation from "../../assets/lottie/dong_logo_animation.android.json";

/** Android uses the same motion with smaller embedded frames to limit startup memory. */
export const splashAnimationSource = splashAnimation;
